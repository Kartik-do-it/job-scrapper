GOOGLE_SHEET_ID=your_google_sheet_id
# Use this on GitHub Actions (store the full JSON as a repository secret).
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-project"}
# For local testing, use the downloaded key file instead of copying it into this file.
# GOOGLE_SERVICE_ACCOUNT_FILE=/absolute/path/to/service-account-key.json
